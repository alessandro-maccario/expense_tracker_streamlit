-- Creating the DB for the expense tracker.
-- This should be done automatically in the Python script in case the DB
-- does not exists. If it exists, than only the connection should happen.
CREATE DATABASE db_expense_tracker;
